---
order: 0
title: Introduction to Number system
description:
---

<Image src={"/svgs/abstract-art-6.svg"} />

## Welcome to Ratio

In this course, you will learn about ratios and how to solve problems
involving them <KnowMoreCard word={"Number system"} meaning={`the properties of operations on numbers, like addition, subtraction, multiplication, and division.`}/>.

<Diagram
  code={`graph TD;
    A-->B;
    A-->C;
    B-->D;
    C-->D;`}
/>

      {/* final button
      <Link className="w-full my-5" href={"2"}>
        <Button
          className="w-full mb-10"
          onClick={async () => {
            const res = await IsLessonCompletedAction("ratio", 1);
            if (res) return;
            lessonCompletedAction("ratio", 1);
            console.log(res);
          }}
        >
          Start Lesson
        </Button>
      </Link> */}
